% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcs_extras.R
\name{banc_edgelist_split}
\alias{banc_edgelist_split}
\title{Compartment-resolved BANC edgelist}
\usage{
banc_edgelist_split(version = c("v2", "v3", "legacy"), overwrite = FALSE)
}
\arguments{
\item{version}{\code{"v2"} (default), \code{"v3"}, or \code{"legacy"} (the
unversioned \code{banc_888_edgelist_split.feather}).}

\item{overwrite}{Logical. If \code{TRUE}, re-download the cached
feather even if it already exists.}
}
\value{
A data frame of compartment-pair connections, columns:
\verb{pre, post, pre_label, post_label, count, norm, post_count, pre_count, connection, pre_conf_nt, pre_conf_nt_p, post_conf_nt, post_conf_nt_p}.
}
\description{
Reads the compiled compartment-to-compartment edgelist
(axon / dendrite / primary_dendrite / primary_neurite / soma /
unknown) for BANC v888 from the public GCS bucket. Sibling of
\code{\link[=banc_edgelist]{banc_edgelist()}} but with \code{pre_label} / \code{post_label} columns and
optional pre/post neurotransmitter prediction.
}
\details{
Source:
\verb{gs://lee-lab_brain-and-nerve-cord-fly-connectome/compiled_data/banc_888/banc_888_edgelist_split_<version>.feather}.
\code{version = "v2"} and \code{version = "v3"} track the synapse-table
generations (see \code{\link[=banc_all_synapses]{banc_all_synapses()}}). The legacy unversioned
\code{banc_888_edgelist_split.feather} is also available via
\code{version = "legacy"}.

Files are ~321-907 MB (v3 is the largest); the first call caches
the feather under \code{tools::R_user_dir("bancr", "cache")}. There is
no CAVE equivalent of this compartment-split edgelist (CAVE only
exposes per-neuron pre/post root pairs), so there's no
\code{source} / \code{fallback} argument.
}
\examples{
\dontrun{
# Compartment edgelist for the paper synapses (v2)
eds <- banc_edgelist_split()

# Axon -> dendrite connections only
library(dplyr)
a2d <- eds \%>\% filter(pre_label == "axon", post_label == "dendrite")
}
}
