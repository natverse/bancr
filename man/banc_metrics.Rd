% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcs_extras.R
\name{banc_metrics}
\alias{banc_metrics}
\title{Per-neuron metrics for BANC v888}
\usage{
banc_metrics(rootids = NULL, overwrite = FALSE)
}
\arguments{
\item{rootids}{Optional vector of root IDs to filter to. \code{NULL}
(default) returns all ~188 k rows.}

\item{overwrite}{Logical. If \code{TRUE}, re-download the cached
feather even if it already exists.}
}
\value{
A data frame of per-neuron metrics.
}
\description{
Reads the compiled per-neuron metrics feather from the
public GCS bucket: cable length, neuron volume, mitochondria volume,
pre/post synapse counts, segregation index and a handful of other
morphology / connectivity scalars (one row per neuron, keyed by
\code{banc_888_id}).
}
\details{
Source:
\verb{gs://lee-lab_brain-and-nerve-cord-fly-connectome/compiled_data/banc_888/banc_888_metrics.feather}
(~7.5 MB; cached under \code{tools::R_user_dir("bancr", "cache")}).
No CAVE equivalent — this table is compiled offline from the
segmentation graph plus the synapse table. There is consequently no
\code{source} / \code{fallback} argument; the only way to refresh is to
re-download with \code{overwrite = TRUE}.
}
\examples{
\dontrun{
m <- banc_metrics()
my_metrics <- banc_metrics(rootids = c("720575941633499884",
                                       "720575941472733451"))
}
}
