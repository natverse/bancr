% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seatabler.R
\name{banctable_columns}
\alias{banctable_columns}
\alias{banctable_add_column}
\alias{banctable_add_columns}
\alias{banctable_delete_column}
\title{Read and write columns of a BANC SeaTable table}
\usage{
banctable_columns(
  table,
  base = NULL,
  workspace_id = "57832",
  token_name = "BANCTABLE_TOKEN",
  include_key = TRUE
)

banctable_add_column(
  table,
  column_name,
  column_type = "text",
  base = NULL,
  column_data = NULL,
  column_key = NULL,
  workspace_id = "57832",
  token_name = "BANCTABLE_TOKEN"
)

banctable_add_columns(
  table,
  columns,
  base = NULL,
  workspace_id = "57832",
  token_name = "BANCTABLE_TOKEN",
  progress = TRUE
)

banctable_delete_column(
  table,
  column_key,
  base = NULL,
  workspace_id = "57832",
  token_name = "BANCTABLE_TOKEN"
)
}
\arguments{
\item{table}{Name of the table.}

\item{base}{Base name or \code{Base} object; discovered from \code{table} when \code{NULL}.}

\item{workspace_id, token_name}{SeaTable connection arguments; see
\code{\link[=banc_seatable_connection]{banc_seatable_connection()}}.}

\item{include_key}{Whether to include the internal SeaTable column key.}

\item{column_name}{Name of the new column.}

\item{column_type}{SeaTable column type, e.g. \code{"text"}, \code{"number"}, \code{"date"},
\code{"checkbox"}, \code{"single-select"}, \code{"multiple-select"}.}

\item{column_data}{Optional list of type-specific settings, e.g. the options
of a select column.}

\item{column_key}{For \code{banctable_add_column()}, the key of an existing column
to insert after. For \code{banctable_delete_column()}, the key of the column to
delete, from \code{banctable_columns(include_key = TRUE)}.}

\item{columns}{A data frame with \code{name} and \code{type} columns. Columns that
already exist are skipped.}

\item{progress}{Whether to report each column as it is added.}
}
\value{
For \code{banctable_columns()}, a data frame with \code{name}, \code{type}, \code{rtype}
and optionally \code{key}.
}
\description{
\code{banctable_columns()} lists a table's columns, their SeaTable
types and the R types they map to. \code{banctable_add_column()} and
\code{banctable_add_columns()} extend a table's schema;
\code{banctable_delete_column()} removes a column and everything in it.
}
\details{
These wrap the generic equivalents in seatabler, supplying the BANC
connection. \code{include_key = TRUE} returns SeaTable's internal column key,
which \code{banctable_delete_column()} needs and which is what SeaTable's error
messages refer to when they name a column like \code{"8blF"}.
}
\examples{
\dontrun{
banctable_columns("banc_meta")
}
}
